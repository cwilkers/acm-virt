namespace: windows-build
resources:
  - namespace.yaml
  - kubevirt-tekton-tasks-okd.yaml
  - windows-efi-installer-pipeline.yaml

configMapGenerator:
  - name: windows11-autounattend
    files:
      - windows11-autounattend/autounattend.xml
